services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: missing

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    pull_policy: missing
